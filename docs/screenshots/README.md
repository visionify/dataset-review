# Screenshots

Add your screenshots here and update the paths in the main README:

- `dashboard.png` — Classes dashboard (stats, class cards)
- `config.png` — Dataset config page (path input / drag-drop)
- `classes.png` — Class cards with sample thumbnails (optional, can reuse dashboard)
- `images.png` — Image grid (All Images or a split)
- `annotation.png` — Single-image annotation view with bboxes
- `validation.png` — Validation checks page
